/** Public marketing home — always browsable (including when license expired). */
export const MARKETING_HOME_PATH = "/";

/** Pricing section on the landing page. */
export const MARKETING_PRICING_HREF = "/#pricing";

/** Optional: force marketing when signed in with a valid license (preview pricing). */
export const MARKETING_HOME_FORCE_LANDING = "/?landing=1";
