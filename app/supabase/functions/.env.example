# Copy to supabase/functions/.env for local serve only — never commit real keys.
# Production: npm run setup:contact-email (or supabase secrets set — see docs/CONTACT_FORM_EMAIL_SETUP.md)
# Optional in app/.env.local for setup script: RESEND_API_KEY=re_…

RESEND_API_KEY=re_your_key
INQUIRY_NOTIFY_TO=support.bikrikhata@gmail.com
RESEND_FROM=BikriKhata <onboarding@resend.dev>
